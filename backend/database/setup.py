from setuptools import setup, find_packages

setup(
    name="database_pkg",             # you can choose any name you like
    version="0.1.0",
    packages=find_packages(),        # this automatically includes 'src'
)
